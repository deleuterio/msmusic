{
  "name": "msmusic",
  "private": true,
  "version": "0.0.0",
  "description": "Marina Soares portifolio.",
  "repository": "",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -p 80 -c-1 ./app &"
  }
}
